{
  "name": "personal-finance-tracker-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Indian Personal Finance & Expense Tracker Monorepo",
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "build": "pnpm --filter \"./packages/*\" build && pnpm --filter \"./apps/*\" build",
    "build:web": "pnpm --filter @personal-finance/types build && pnpm --filter @personal-finance/shared build && pnpm --filter web build",
    "build:api": "pnpm --filter @personal-finance/types build && pnpm --filter @personal-finance/shared build && pnpm --filter api build",
    "test": "pnpm --filter \"./packages/*\" test",
    "typecheck": "pnpm --recursive typecheck",
    "lint": "pnpm --recursive lint",
    "db:generate": "prisma generate --schema=prisma/schema.prisma",
    "db:push": "prisma db push --schema=prisma/schema.prisma",
    "db:seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.4.1"
  },
  "devDependencies": {
    "prisma": "^6.4.1",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.7"
  }
}
